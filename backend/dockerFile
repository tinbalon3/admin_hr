# Sử dụng Python 3.13.1-slim-bullseye từ Docker Hub (nếu có, nếu không hãy thay bằng phiên bản phù hợp)
FROM python:3.13.1-slim-bullseye

# Vô hiệu hóa tạo file .pyc và bật chế độ unbuffered output cho Python
ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

# Cài đặt các gói hệ thống cần thiết
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    libffi-dev \
    libpq-dev \
    python3-dev \
    && rm -rf /var/lib/apt/lists/*

# Thiết lập thư mục làm việc trong container
WORKDIR /app

# Sao chép file requirements.txt vào thư mục làm việc trong container
COPY requirements.txt /app/

# Cài đặt các package Python cần thiết từ file requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Sao chép toàn bộ mã nguồn backend (bao gồm app/, alembic/, v.v.) vào container
COPY . /app

# Mở cổng 8000 để ứng dụng FastAPI có thể truy cập từ bên ngoài
EXPOSE 8000

# CMD: Chạy migration với Alembic, sau đó khởi động server FastAPI
CMD alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000
